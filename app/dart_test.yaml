tags:
  # Hits a real backend on localhost:3000. Excluded from the default run so
  # `flutter test` stays hermetic; run with `flutter test --tags live`.
  live:
    skip: "needs a running backend — run with: flutter test --tags live"
